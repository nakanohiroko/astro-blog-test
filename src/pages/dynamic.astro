---
import Layout from "../layouts/Layout.astro";
export const prerender = false; // このページを動的にレンダリング
const title = "動的ページ";
const currentTime = new Date().toLocaleString();
---

<Layout>
  <main class="main">
    <h1>{title}</h1>
    <p>現在の時刻: {currentTime}</p>
  </main>
</Layout>